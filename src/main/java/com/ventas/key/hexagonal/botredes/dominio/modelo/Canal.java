package com.ventas.key.hexagonal.botredes.dominio.modelo;

/** Comentario público en una publicación, o mensaje directo (Instagram DM / Messenger). */
public enum Canal {
    COMENTARIO,
    MENSAJE_DIRECTO
}
