
server:
  port: 9091
  servlet:
    context-path: /mis-productos
---

spring:
  codec:
    max-in-memory-size: 10MB
  cache:
    #type: redis
    type: simple
  redis:
    host: 127.0.0.1
    port: 6379
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://51.178.29.99:3306/inventario_key_qa
#    url: jdbc:mysql://db-bolsas-key.cuf6ai4u4jfs.us-east-1.rds.amazonaws.com:3306/inventario_key
#    url: ${SPRING_DATASOURCE_URL}
#    username: ${SPRING_DATASOURCE_USERNAME}
#    username: admin
    username: user_ventas_qa
#    password: ${SPRING_DATASOURCE_PASSWORD}
#    password: tpM9EBgFzQ![<NTfvJu2g$6S9Ele
    password: Luvianos *13
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 60000         # 1 min: libera conexiones idle rápido en VPS con poca RAM
      max-lifetime: 300000        # 5 min: rota conexiones antes de que MySQL las corte
      connection-timeout: 20000   # 20 seg: tiempo máximo esperando una conexión del pool
      keepalive-time: 30000       # 30 seg: ping para mantener la conexión viva
      connection-test-query: SELECT 1
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: mail.novedades-jade.com.mx
    port: 587
    username: ${MAIL_USERNAME:qa.boutique.bolsas@novedades-jade.com.mx}
    password: ${MAIL_PASSWORD:NovedadesJade *13}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.starttls.required: true

clave-seguridad:
  clave: miClaveSuperSeguraDe32Caracteres

cookie:
  secure: false  # true en producción (requiere HTTPS)

api:
#  imagenes: http://k8s-default-imagenes-65ff6abba3-120698284f8297cf.elb.us-east-1.amazonaws.com/mis-productos
  imagenes: http://localhost:9096/mis-productos
  #imagenes: https://backend-imagenes.novedades-jade.com.mx/mis-productos
  cors_angular: http://localhost:4200
#logging:
#  level:
#    org.hibernate.SQL: DEBUG
#    org.hibernate.orm.jdbc.bind: TRACE

logging:
  level:
    root: INFO
    com.miempresa: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: caches,health

guardar-imagenes:
  ruta_imagenes: D:\\Imagenes

pedidos:
  dias-limite-recogida: 2


redis:
  dns: 127.0.0.1

openai:
  api-key: asdasdsa
  model: gpt-4o-mini

mercadopago:
  access-token: asdasd
  device-id: aaaa
  sandbox: false

chat:
  admin-email: ${CHAT_ADMIN_EMAIL:qa.boutique.bolsas@novedades-jade.com.mx}
  forzar-notificacion: true

whatsapp:
  # Proveedor activo: callmebot | twilio | ninguno
  proveedor: ${WHATSAPP_PROVEEDOR:callmebot}
  # CallMeBot: API key generada al activar el bot (gratis, requiere activación por cliente)
  callmebot-apikey: ${WHATSAPP_APIKEY:}
  # Prefijo de país para números sin código (+52 = México)
  country-code: ${WHATSAPP_COUNTRY_CODE:52}


